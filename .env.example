# ── State (shared across workflow steps; required for build-list etc.) ─
# Absolute path to state dir so apollo-match/bouncer-verify see apollo-search output.
# Example: OPENCLAW_STATE_DIR=/home/os/openclaw-mvp/state
OPENCLAW_STATE_DIR=/path/to/your/openclaw-mvp/state

# ── LLM (agent + reply classification) ────────────────────────
OPENAI_API_KEY=your-openai-api-key-here

# ── Slack (channels.slack + daily-report) ─────────────────────
SLACK_APP_TOKEN=xapp-your-app-token
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_REPORT_CHANNEL=C01234ABCDE

# ── apollo-search + apollo-match + bouncer-verify ────────────
APOLLO_API_KEY=your-apollo-api-key
APOLLO_BATCH_SIZE=1
BOUNCER_API_KEY=your-bouncer-api-key

# ── instantly-load + instantly-fetch ─────────────────────────
INSTANTLY_API_KEY=your-instantly-api-key-base64
INSTANTLY_CAMPAIGN_ID=your-campaign-uuid

# ── Supabase (database persistence + migrations) ──────────────
# One connection string is enough: skills use it to read/write (pg); migrations use it for psql.
# Dashboard → Settings → Database → Connection string → URI (use pooler for serverless)
SUPABASE_DB_URL=postgresql://postgres.[ref]:[PASSWORD]@aws-0-[region].pooler.supabase.com:5432/postgres
