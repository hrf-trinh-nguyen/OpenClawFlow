# ── OpenClaw Gateway ──────────────────────────────────────────
# Copy this file to:
# - Local dev: /home/os/openclaw-mvp/.env
# - VPS:       /home/deploy/openclaw-mvp/.env
#
# Optional if `openclaw` is not on PATH in your service environment:
# OPENCLAW_BIN=/home/linuxbrew/.linuxbrew/bin/openclaw
#
# Generate with: openssl rand -hex 24
OPENCLAW_GATEWAY_TOKEN=your-random-hex-token-here

# ── State (shared across workflow steps; required for build-list etc.) ─
# Absolute path to state dir so apollo-match/bouncer-verify see apollo-search output.
# Local:  OPENCLAW_STATE_DIR=/home/os/openclaw-mvp/state
# VPS:    OPENCLAW_STATE_DIR=/home/deploy/openclaw-mvp/state
OPENCLAW_STATE_DIR=/path/to/your/openclaw-mvp/state

# ── LLM (agent + reply classification) ────────────────────────
OPENAI_API_KEY=your-openai-api-key-here
# Reply classification: gpt-4o (default, recommended for accuracy), or gpt-4 / gpt-4-turbo
# REPLY_CLASSIFICATION_MODEL=gpt-4o

# ── Slack (channels.slack + daily-report) ─────────────────────
SLACK_APP_TOKEN=xapp-your-app-token
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_REPORT_CHANNEL=C01234ABCDE   # step reports + daily report channel
SLACK_ALERT_CHANNEL=C01234ABCDE   # error/alert channel (can be different)

# ── apollo-search + apollo-match + bouncer-verify ────────────
APOLLO_API_KEY=your-apollo-api-key
APOLLO_BATCH_SIZE=1
BOUNCER_API_KEY=your-bouncer-api-key
# Bouncer cron: max leads to verify per day (default 300)
BOUNCER_DAILY_CAP=300

# ── instantly-load + instantly-fetch ─────────────────────────
INSTANTLY_API_KEY=your-instantly-api-key-base64
INSTANTLY_CAMPAIGN_ID=your-campaign-uuid
# Max leads to push to Instantly per day PT (default in code: 250)
INSTANTLY_LOAD_DAILY_CAP=250

# ── Supabase (database persistence + migrations) ──────────────
# One connection string is enough: skills use it to read/write (pg); migrations use it for psql.
# Dashboard → Settings → Database → Connection string → URI (use pooler for serverless)
SUPABASE_DB_URL=postgresql://postgres.[ref]:[PASSWORD]@aws-0-[region].pooler.supabase.com:5432/postgres
